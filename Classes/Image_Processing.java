

public class Image_Processing {

	
	/**
	 * Here you need to process the image so it's better for OCR
	 * 
	 * Return the Name and Location of the edited image so it can then be reprocessed for OCR
	 * 
	 * @param FileNameAndLocation
	 * @return
	 */
	
	public static String processImage(String FileNameAndLocation) {

		return FileNameAndLocation;
		
	}
}
